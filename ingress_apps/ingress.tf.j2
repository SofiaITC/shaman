resource "aws_route53_record" "wildcard-apps" {
  name = "*.apps.${data.aws_route53_zone.private_zone.name}"
  type = "CNAME"
  zone_id = data.aws_route53_zone.private_zone.zone_id
  records = ["{{ elb_name }}"]
  ttl = 300
}
